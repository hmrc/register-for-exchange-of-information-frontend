{% macro title(mainTitle, errors = []) %}
  {% set titlePrefix = messages("error.browser.title.prefix") if errors.length > 0 else "" %}

  {{ titlePrefix + ' ' + mainTitle + ' ' + messages("site.govuk") }}
{% endmacro %}
